"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "node scripts/bootstrap/retire-integration-database-url.mjs && pnpm install --frozen-lockfile && pnpm --dir backend run build:workspace-deps"
run_mode = "nonconcurrent"

[scripts.run.common-db-dev]
available_in = [ "local" ]
command = "scripts/run-common-db-dev.sh"
default = true
icon = "database"
