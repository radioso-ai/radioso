---
title: Authentication
description: Session, API token, public-chat, and Enterprise embed access patterns in Radioso.
---

import { Steps } from 'nextra/components'

# Authentication

Radioso supports more than one authentication model because the product serves more than one surface. The web app, API, public chat, and Enterprise embed flows do not use the same credentials.

## Main access modes

- Session-based access for the product UI
- Workspace API tokens for backend-to-backend or developer automation
- Anonymous public chat and Enterprise embed flows for customer-facing agent experiences

## Session-based access

Use sessions when a person is interacting with the product UI.

The main auth routes are:

- register
- login
- invitation accept

Registration creates the account and default workspace, then sends a verification email. The backend sets the session cookie on successful login, invitation acceptance, and password reset confirmation. Open-source Radioso includes password reset and email verification endpoints, and normal sign-in requires a verified email address.

## Workspace API tokens

Use workspace tokens when you want to call the workspace-scoped API without a browser session. A common pattern is:

<Steps>
### Sign in with a normal user account

Use session auth to reach the account and workspace APIs.

### Reveal the workspace token

Fetch the workspace token from the authenticated workspace route.

### Call workspace-scoped APIs

Use the bearer token for document, chat, settings, and related automation flows.
</Steps>

Workspace tokens are high-value credentials. If one leaks, rotate it from workspace settings rather than trying to work around the exposure.

## Public chat and embed access

Public website chat does not use a normal operator session. Instead, it relies on workspace-controlled public settings, public chat tokens, or embed session flows that are scoped to the workspace configuration.

This is the right model for customer-facing agents, not for operator automation. The hosted website widget is an Enterprise Edition surface; anonymous public chat uses the same public-session model without the widget routes.

## Choose the right mode

- Use sessions for humans in the app.
- Use workspace bearer tokens for automation.
- Use public chat or Enterprise embed flows for customer-facing agents.

## Start points

- [API first success](/quickstarts/api-first-success)
- [API Reference](/api-reference)
- [Public chat and embed API](/api/public-chat-and-embed)
