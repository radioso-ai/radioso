{
  "name": "radioso-ee",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --filter './packages/*' run build",
    "test": "pnpm --filter './packages/*' run test"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
