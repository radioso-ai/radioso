project_id        = "radioso-staging"
state_bucket_name = "radioso-staging-terraform-state"

# Optional overrides (defaults shown)
# region = "us-central1"

# Live example:
# project_id        = "radioso"
# state_bucket_name = "radioso-terraform-state"
