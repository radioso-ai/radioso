## What this changes

A short description of the change and the problem it solves. Link the issue it
closes (`Closes #123`).

## How it was tested

- [ ] `pnpm run ci:local -- origin/main` (paste the result below)
- [ ] Added or updated tests for the change
- [ ] Updated docs, if this affects setup, auth, APIs, ingestion, retrieval, SDK, or MCP usage
- [ ] Regenerated the SDK snapshot (`cd typescript-sdk && pnpm run sync`), if this changes the API contract

```
# ci:local output
```

## Notes for reviewers

Anything worth calling out — trade-offs, follow-ups, or areas that need a closer
look.
