---
title: Connectors
description: Read connector state, save connector config, enable or disable connectors, and run manual syncs.
---

# Connectors

Connectors link a workspace to external systems. The exact config shape depends on the connector type.

## Relevant endpoints

```text
GET  /api/v1/connectors
GET  /api/v1/connectors/{connectorId}
PUT  /api/v1/connectors/{connectorId}
POST /api/v1/connectors/{connectorId}/enable
POST /api/v1/connectors/{connectorId}/disable
POST /api/v1/connectors/{connectorId}/sync
```

## How it works

1. List the connectors available to the workspace.
2. Read one connector's current detail.
3. Save connector-specific config.
4. Enable the connector when the config is ready.
5. Run a manual sync when the connector supports it.

The key point is that connector config is not one global schema. The body depends on the connector you are configuring, so the [API Reference](/api-reference) is the authoritative place for the exact shape.

Connector detail responses also include `syncState`. This shows the latest backfill completion time, latest sync attempt, upstream modified cursor, count ingested by the last sync, and the latest connector error status.

## Save connector config

Use:

```text
PUT /api/v1/connectors/{connectorId}
```

Then enable it with:

```text
POST /api/v1/connectors/{connectorId}/enable
```

Disable it with:

```text
POST /api/v1/connectors/{connectorId}/disable
```

## Run a manual sync

Use:

```text
POST /api/v1/connectors/{connectorId}/sync
```

The response returns the number of documents ingested by that sync:

```json
{
  "ingested": 12
}
```

Not every connector supports manual sync. Unsupported connectors return `409`.

## Common failure modes

- `400` usually means the connector config is incomplete or invalid.
- `401` means the authenticated workspace context is missing for connector-management routes.
- `404` means the connector or workspace was not found.
- `409` usually means the connector is in a conflicting state for the requested save or enable action, or that the connector does not support manual sync.

## Read next

- [Public chat and embed](/api/public-chat-and-embed)
- [API Reference](/api-reference)
