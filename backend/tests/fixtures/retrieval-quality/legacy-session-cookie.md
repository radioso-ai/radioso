# Legacy Session Cookie

Browser sessions use an HTTP-only cookie named `radioso_session`.

This fixture is used to simulate older chunks that have not been backfilled with `search_text` or structured attributes yet.
